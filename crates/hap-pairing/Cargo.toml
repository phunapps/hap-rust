[package]
name = "hap-pairing"
version = "0.0.0"
description = "HomeKit Accessory Protocol pairing orchestration: Pair Setup / Pair Verify state machines, pairing management, and persistence."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
keywords = ["homekit", "hap", "pairing", "state-machine", "iot"]
categories = ["authentication", "network-programming"]
readme = "README.md"

[lints]
workspace = true

[dependencies]
thiserror = { workspace = true }
hap-tlv8 = { path = "../hap-tlv8", version = "0.0.0" }
hap-crypto = { path = "../hap-crypto", version = "0.0.0" }
hap-transport = { path = "../hap-transport", version = "0.0.0" }
