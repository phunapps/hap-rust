# /accessories test vectors

Captured (or spec-faithful hand-authored) bodies of `GET /accessories`
responses, used by `hap-model`'s integration tests.

| file           | provenance                                                                                          |
| -------------- | --------------------------------------------------------------------------------------------------- |
| lightbulb.json | Hand-authored, spec-faithful single-LightBulb payload (Accessory Information + LightBulb service). Not captured from hardware; created for M6 to exercise `parse_accessories` against a representative tree. Replace with a real capture when one is recorded. |
